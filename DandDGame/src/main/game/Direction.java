package main.game;

/**
 * Enums to direct moving on map.
 */
public enum Direction {
	/** move up on map. */
	North,

	/** move down on map. */
	South, 
	
	/** move left on map. */
	East, 
	
	/** move right on map. */
	West, 
	
	/** stay in same position. */
	CurrentPosition;
}
